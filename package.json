{
  "name": "HUD",
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix client",
    "dev:backend": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix client",
    "kill:backend": "lsof -ti:3001 | xargs kill -9 2>/dev/null; echo 'port 3001 cleared'"
  }
}
